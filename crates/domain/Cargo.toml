[package]
name = "domain"
version = "0.1.0"
edition = "2021"

[dependencies]
uuid = { version = "1", features = ["v4"] }
rust_decimal = "1"
chrono = { version = "0.4", features = ["serde"] }
thiserror = "1"
